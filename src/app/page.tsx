export default function Home() {
  // ⚠️ EMPTY HOMEPAGE — Pod must design from scratch.
  //
  // The template intentionally ships with NO UI scaffolding (no hero, stats, cards,
  // search bar, etc.) to prevent visual homogenization across the 100-site empire.
  //
  // Each site needs its own identity: read STYLEGUIDE.md, study the data shape,
  // pick reference designs, and build the homepage that fits THIS product.
  //
  // Delete this comment block and write your own homepage.
  return null;
}
